import "./legal.css";

// Policy pages: privacy, terms, shipping & returns. Each page renders the shared
// <LegalPage> frame (header, page tabs, notice, article).
export default function LegalLayout({ children }: { children: React.ReactNode }) {
  return children;
}
